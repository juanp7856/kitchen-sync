# OpenSpec Configuration — kitchen-sync
# Auto-generated by sdd-init-dev-go

context: >
  KitchenSync es una herramienta de sincronización de proyectos en tiempo real
  basada en la metáfora de una Cocina Profesional. Next.js 16 App Router,
  React 19, TypeScript 5 strict, Supabase (PostgreSQL + Realtime), Tailwind CSS 4.
  Sistema de sesiones Lunes/Viernes con clonado de platos y trazabilidad.

strict_tdd: true

testing:
  runner: vitest
  runner_command: npm test
  watch_command: npm run test:watch
  layers:
    unit: vitest + React Testing Library
    integration: vitest + jsdom
    e2e: false
  coverage:
    available: true
    command: vitest run --coverage
  quality:
    linter: eslint
    linter_command: npm run lint
    type_checker: typescript
    type_checker_command: npx tsc --noEmit
    formatter: false

phases:
  proposal:
    requires_spec: false
    rules: []
  spec:
    requires_approval: true
    rules:
      - All specs must reference the kitchen metaphor terminology
  design:
    requires_approval: true
    rules: []
  tasks:
    requires_approval: false
    rules:
      - Max 400 lines per review budget
  apply:
    requires_approval: false
    rules: []
  verify:
    requires_approval: false
    rules:
      - Must pass vitest full suite before marking complete
  archive:
    requires_approval: false
    rules: []
